import { APIModule } from './base';
import { Message, MessageOptions } from '../types';
export declare class Chat extends APIModule {
    delete: (channel: string, ts: string, options?: {
        asUser?: boolean;
    }) => Promise<{
        channel: string;
        ts: string;
    }>;
    meMessage: (channel: string, text: string) => Promise<{
        channel: string;
        ts: string;
    }>;
    postEphemeral: (channel: string, message: Message, options?: MessageOptions) => Promise<{
        messageTs: string;
    }>;
    postMessage: (channel: string, message: Message, options?: MessageOptions) => Promise<{
        channel: string;
        ts: string;
    } & {
        message: Message;
    }>;
    update: (channel: string, ts: string, message: Message) => Promise<any>;
}
