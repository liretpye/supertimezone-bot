export declare type UserMessage = {
    id: string;
    text: string;
    channel: string;
    user: string;
};
export declare type ParseFun<T> = (reply: string) => T;
export declare type ParseObj<T> = {
    parse: ParseFun<T>;
    isValid(parsed: T): boolean;
};
export declare type ParseType<T> = ParseFun<T> | ParseObj<T>;
